interface:
  display_name: "YouTube Trading Summary"
  short_description: "Fetch and summarize trading info from any YouTube video"
  default_prompt: "Use rhino-finance-latest to fetch the latest YouTube video (defaults to Rhino Finance), obtain transcript or captions, translate the content into Chinese, and extract all trading information."

policy:
  allow_implicit_invocation: true
